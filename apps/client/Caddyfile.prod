:80 {
    root * /usr/share/caddy

    # No API proxy - client calls server directly for proper cookie handling
    # This avoids cookie domain issues with proxied requests

    # SPA fallback - all routes serve index.html for client-side routing
    try_files {path} /index.html

    file_server
}
